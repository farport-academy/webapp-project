import { ObjToStrArrPipe } from './obj-to-str-arr.pipe';

describe('ObjToStrArrPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjToStrArrPipe();
    expect(pipe).toBeTruthy();
  });
});
